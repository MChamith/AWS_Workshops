{
  "name": "users",
  "version": "1.0.0",
  "main": "lambda_function.ts",
  "scripts": {
    "build": "esbuild lambda_function.ts --bundle --minify --sourcemap --platform=node --external:aws-sdk --target=es2020 --outfile=dist/lambda_function.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/aws-lambda": "^8.10.157",
    "axios": "^1.13.2",
    "esbuild": "^0.25.12",
    "jsonwebtoken": "^9.0.2",
    "jwk-to-pem": "^2.0.7",
    "uuid": "^13.0.0"
  }
}
